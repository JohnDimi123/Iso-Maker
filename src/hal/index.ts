/** Hardware abstraction layer public API. */
export { DriveManager, driveManager } from './drive-manager';
export type { DriveBackend } from './types';
export { writeSpeedsFor, familyOf } from './util';
